apiVersion: v1
kind: Service
metadata:
  name: orcprocess-service
spec:
  selector:
    app: core
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30085
  type: NodePort

